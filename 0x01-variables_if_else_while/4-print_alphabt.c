#include <stdio.h>
/**
* main - print the alphabet letter except q and e
* Return: 0
*/
int main(void)
{
char = abc;
while (abc != 'q' && abc != 'e')
{
for (abc = 'a'; abc <= 'z'; abc++)
{
putchar(abc);
}
putchar('\n');
}
return (0);
}
