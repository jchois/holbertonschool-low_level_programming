#include <stdio.h>

/**
* main - print alphabet in lowercase
* Return: 0 always
*/
int main(void)
{
char alphabet;
for (alphabet = 'a' ; alphabet <= 'b' ; alphabet++)
{
putchar (alphabet);
}
return (0);
}
