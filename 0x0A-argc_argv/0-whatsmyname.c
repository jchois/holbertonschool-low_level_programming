#include <stdio.h>

/**
 * main - print its name, followed by a new line
 * @argc: asd
 * @argv: asd
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
